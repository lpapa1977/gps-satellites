# GPS Satellites

Android app that shows visible GNSS satellites in a sky plot using azimuth and elevation from Android's `GnssStatus` API.

Green satellites are used in the current location fix. Yellow satellites are visible but not currently used for the fix.

Build:

```bash
gradle assembleDebug
```
